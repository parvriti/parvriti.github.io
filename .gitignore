.DS_Store
*.log
.preview-tmp/

# private source material for the "Parv" section — kept out of the public repo
riti_letters/
riti-letters-ocr.txt
# authoring source for the Open When letters (bodies live inline in js/open-when.js);
# keep local only, never serve it publicly via Pages
open-when-letters.md

# secrets — must never be committed to this public repo
.env
*.env
.dev.vars
*firebase-adminsdk*.json
*service-account*.json

# wrangler local build cache — regenerated on every deploy, never committed
.wrangler/
push-worker/.wrangler/

# personal room photos — never public; they live in Firestore instead
our_pics/
room-thumbs/

# local dev (Claude Code launch config)
.claude/

# archived older implementations — local reference only, never deployed
old_ideas/

# handoff archives contain personal data (seed-cycle.mjs) — never commit
parvriti-handoff*
*.tar.gz

# never commit PDFs (personal exports, health data, etc.)
*.pdf

# local mirror of the live Firestore rules (kept current, published by hand
# in the Firebase console — not deployed from here). Local reference only.
firestore-rules/

# local QA harnesses (generated firebase-stub copies of pages — never ship)
_qa_*.html
